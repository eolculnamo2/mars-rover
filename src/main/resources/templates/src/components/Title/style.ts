import styled from 'styled-components';
import { ROVER_PURPLE } from '../../shared/styled-variables';

export const TitleHeader = styled.h1`
  color: ${ROVER_PURPLE};
  text-align: center;
  text-decoration: underline;
`;
